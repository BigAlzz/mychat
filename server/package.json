{
  "name": "llm-conversation-app-server",
  "version": "1.0.0",
  "description": "Backend server for local file search",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-client": "cd .. ; npm run build",
    "install-client": "cd .. ; npm install",
    "deploy": "cd .. ; npm install ; npm run build ; cd server ; npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
